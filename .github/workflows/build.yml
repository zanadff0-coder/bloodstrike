name: Build Android NDK Project

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install Android NDK r21e
      run: |
        wget -q https://google.com
        unzip -q android-ndk-r21e-linux-x86_64.zip
        echo "ANDROID_NDK_HOME=$GITHUB_WORKSPACE/android-ndk-r21e" >> $GITHUB_ENV

    - name: Unzip and Fix JNI Path
      run: |
        unzip -q "bloodstrike src 013 (1).zip"
        MK_FILE=$(find . -name "Android.mk" | head -n 1)
        if [ -z "$MK_FILE" ]; then
          echo "Error: Android.mk not found!"
          exit 1
        fi
        JNI_DIR=$(dirname "$MK_FILE")
        echo "PROJ_DIR=$(dirname "$JNI_DIR")" >> $GITHUB_ENV

    - name: Compile C++ Code with ndk-build
      run: |
        cd ${{ env.PROJ_DIR }}
        $ANDROID_NDK_HOME/ndk-build

    - name: Upload Output Compiled .so Files
      uses: actions/upload-artifact@v4
      with:
        name: BloodStrike-Bypass-SO
        path: |
          **/libs/arm64-v8a/*.so
          **/libs/armeabi-v7a/*.so
